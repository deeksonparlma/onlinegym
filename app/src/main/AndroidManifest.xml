<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.epicodus.gym">
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/puch"
        android:label="@string/app_name"
        android:roundIcon="@drawable/puch"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.">
        <activity android:name=".UI.liveGym"
            android:launchMode="singleTop"
            android:screenOrientation="landscape"></activity>
        <activity android:name=".UI.userStats"
            android:launchMode="singleTop"/>
        <activity
            android:name=".UI.HomeScreen"
            android:launchMode="singleTop" />
        <activity
            android:name=".Authentication.Authentication"
            android:launchMode="singleTop" />
        <activity
            android:name=".UI.MainActivity"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>